@using OnlineDoctorsAppointmentApp.Models
@model IEnumerable<OnlineDoctorsAppointmentApp.Models.VisitingSession>

@{
    ViewBag.Title = "Search Doctor";
}

<h2>Search Doctor</h2>



<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.Doctors.DoctorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctors.Specialization)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Chambers.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Chambers.Zone)
        </th>
        <th>
            Visiting Time
        </th>
        <th>
            No. Of Available Seat
        </th>
        <th>
            Action
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doctors.DoctorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctors.Specialization)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Chambers.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Chambers.Zone)
            </td>
                <td>
                @Html.DisplayFor(modelItem => item.StartTime)
            </td>
            <td>
                @{
                    int count = 50;
                   List<Appointment> appointment = new List<Appointment>();
                    
                   foreach (Appointment anAppointment in appointment)
                   {
                       if (anAppointment.DoctorId == item.Doctors.DoctorId)  // problem is here anappointment.DoctorID 
                       {
                           count++;
                       }
                   }

                   //ViewBag.NoOfAppoinments
                  // ViewBag.available = item.Doctors.DoctorId;
                   //ViewBag.available = item.MaxNoOfAppointments - count;
                    ViewBag.available = count; 
                    
                    
                }
                 @ViewBag.available
                @*@Html.DisplayFor(modelItem => item.MaxNoOfAppointments)*@
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.VisitingSessionId }) |
                @Html.ActionLink("Details", "Details", new { id = item.VisitingSessionId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.VisitingSessionId })*@
                
                <input id="appointmentButton" type="button" value="Confirm" /> 
            </td>
        </tr>
    }

</table>

